- The following files pertaining to the mosaic in question needs to in the directory where 'runfile.sh' is to be run:

image.restored.i.<SB number>.cube.contsub.fits
selavy-image.i.<SB number>.cont.taylor.0.restored.islands.txt .

- Please replace the <SB number> with the correct values in runfile.sh .

- Need to use Python 3.

- Final output are a set of png files.
